#ifndef MAKEFILE_H
#define MAKEFILE_H

#include <string>
#include <fstream>

class Makefile {
    private:
    
    public:
    void create_makefile(std::string);
    void init_makefile(std::string);
};

#endif